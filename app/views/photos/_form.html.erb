<%= form_for @photo, html: {multipart: true} do |f| %>
<%#= form_for @photo, html: {multipart: true}, remote: true do |f| %>
  <% if @photo.errors.any? %>
    <div data-alert class="alert-box alert">
     <%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:
      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
      <a href="#" class="close">&times;</a>
    </div>
  <% end %>

<%= f.hidden_field :key %>

<div class="row collapse">
  <div class="small-3 columns"><%= f.label "Date", class: "right inline" %></div>
  <div class="small-9 columns"><%= f.date_select :when_taken, {order: [:year, :month, :day]}, {class: "small-2"} %></div>
</div>
<div class="row collapse">
  <div class="small-3 columns"><%= f.label "Title", class: "right inline" %></div>
  <div class="small-9 columns"><%= f.text_field :caption %></div>
</div>
<div class="row collapse">
  <div class="small-3 columns"><%= f.label :description, class: "right inline" %></div>
  <div class="small-9 columns"><%= f.text_area :description %></div>
</div>
<div class="row collapse">
  <div class="small-3 columns"><%= f.label File, class: "right inline" %></div>
  <div class="small-9 columns"><div class="subheader"><%= @photo.image_file_name %></div></div>
</div>
</br>
<div class="row collapse">
  <div class="small-9 small-offset-3 columns"><%= f.submit class: "button round small success" %>
<% end %>
